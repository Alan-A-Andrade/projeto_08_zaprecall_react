import TurnIcon from "../assets/turn.png"

export default function FlashcardFooter(Props){
    return(
        <nav className="flashcard-footer">
            <img src={TurnIcon} />
            <h1>ISSOTEMQUEFICARNOBOTTOM</h1>
        </nav>


    )
}

